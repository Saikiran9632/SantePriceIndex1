<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SantePriceIndex">

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.SantePriceIndex">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name" />

        <activity
            android:name=".ProfitCalculatorActivity"
            android:exported="false"
            android:label="Profit Calculator" />
        <activity
            android:name=".PriceWatchActivity"
            android:exported="false"
            android:label="Price Watch" />
        <activity
            android:name=".PriceBoardActivity"
            android:exported="false"
            android:label="Price Board" />
    </application>

</manifest>
